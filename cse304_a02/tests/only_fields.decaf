class IntList{
    public boolean search(int v) {
        if (this.value == v)
            {
                return true;
            }
        else
            if (next == null)
                { /* end of list, so search fails */
                    return false;
                }
            else /* end of list, so search fails */
                return next.search(v);
    }
}
